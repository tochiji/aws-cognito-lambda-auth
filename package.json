{
  "name": "delete-test-01",
  "description": "aws-cognito-lambda-auth",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-jwt-verify": "^3.1.0",
    "aws-sdk": "^2.799.0"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest"
  }
}
